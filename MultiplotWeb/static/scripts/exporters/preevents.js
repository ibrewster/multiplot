//TODO: placeholder for now. Need to do some testing and implementation, hopefully a generic for preevents.
//export class RemoteSensing {
    //static HotLINKRadiativePower(plotDiv){
        //const exportData=plotDiv.data('exportData');

        //const types=[],dates=[],values=[];
        //const headers=['type','date','value'];
        //for(const type in exportData){
            //let date,value;
            //[date,value]=exportData[type];
            //for(let i=0;i<date.length;i++){
                //types.push(type);
                //dates.push(date[i]);
                //values.push(value[i]);
            //}
        //}

        //return [headers,[types,dates,values]];
    //}
//}
